# Fork of kle-ng: do not ship upstream cloud (PCB API / Supabase GitHub login).
# Leave these unset so Continue with GitHub stays hidden and PCB generation
# is disabled until we run our own backends.
# VITE_BACKEND_URL=
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
